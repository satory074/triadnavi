# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

**triadnavi** — FF14 のミニゲーム「トリプルトライアド」の最善手ナビ。ユーザーが FF14 本体で対戦しながら、有効なルールとカードの四方向の数字を都度入力すると、盤面を最後まで読み切って次の一手を示す静的Webアプリ(画面読み取りや自動操作はしない)。対戦前には、登録した手持ちのカードから、相手の NPC に対して最も勝てるデッキを探せる。GitHub Pagesで公開。

## Commands

```bash
npm run dev          # ローカル開発 (http://localhost:5173/triadnavi/)
npm run build        # 型チェック(tsc -b) + 本番ビルド
npm test             # Vitest (コアロジックとデータの単体テスト)
npm run lint         # oxlint
npm run preview      # ビルド成果物の確認 (http://localhost:4173/triadnavi/)
npm run bench        # 探索のベンチマーク(VITE_BENCH=1 で bench.test.ts を有効化)
npm run bench:deck   # デッキ 1 つの評価にかかる時間を、実在の NPC で測る(deckBench.test.ts。数分かかる)
npm run data:update  # カード/NPC/入手方法/アチーブメント/大会のデータの更新(手動実行。生成された src/data/*.json をコミットする)
```

`npm install` が `Cannot read properties of null (reading 'edgesOut')` で落ちる場合は npm 10.9.2 の依存解決の不具合。`package-lock.json` がある状態なら通る。

## Architecture

**Vite + React 19 + TypeScript strict。** ロジックは `src/core/` に純粋TSモジュールとして分離されており、React非依存。乱数は `core/rng.ts` の `makeRng` を注入する(core は `Math.random` を読まない)。テストはコロケーション(`*.test.ts`)。`erasableSyntaxOnly` のため enum は使わない。

- `core/types.ts` — `CardDef {sides:[上,右,下,左], type}`、`Player`(0=自分/青, 1=相手/赤)、`RuleSet`
- `core/rules.ts` — ゲームデータのルール ID(1〜15)との対応、排他 3 組、高速エンジン用のビット、ルールの説明文(`RULE_HELP`)
- `core/refEngine.ts` — **参照エンジン**。不変データの読みやすい実装で、裏返りの理由(セイム/プラス/コンボ何連鎖目)を返す。**UI はこちらを使う**
- `core/fastEngine.ts` — **高速エンジン**。型付き配列とビットマスクを `place()`/`undo()` で書き換える探索専用の実装
- `core/search.ts` — 全深さの negamax + アルファベータ、幅ゼロの窓の探り、相手の応手ごとの保証結果の集計
- `core/position.ts` — ソルバーの唯一の入力 `Position`、`guaranteeKind()`(exact / pool / estimate / chaos)、高速エンジンへの変換
- `core/analyze.ts` — タスク単位の純関数 `runTask()`(1 タスク = 初手 1 つ × 計算 1 種類)
- `core/scheduler.ts` — タスクの優先順と結果の反映(純関数)。`core/analyzeSync.ts` はワーカーなしで最後まで解く版(テスト用とフォールバック)
- `core/rank.ts` — 手の順位付けと日本語のラベル
- `core/worlds.ts` — 相手の具体的な手札(とカオスの引き順)の列挙/サンプリング。候補が足りない分は `fill`(`HandFill`)で埋める
- `core/handPrior.ts` — 相手の裏向きの手札の想定 `HandPrior`(`level` = 3 段階 / `meta` = 大会のプレイヤー / `draft` = ドラフトの相手)と、想定から不明スロットを埋めるサンプラー `makeHandSampler`。対局中(worlds)とデッキ評価(deckEval)が同じものを使う
- `core/chaos.ts` — カオスの厳密な期待値計算と悲観的下界
- `core/match.ts` — 対局の記録(イベントソーシング)。`replay()` は参照エンジンで再生する。`unrevealCard()` は開き間違えたカードを戻す(記録を書き換える唯一の関数)
- `core/suddenDeath.ts` — サドンデス再戦の手札の組み直し
- `core/appState.ts` / `core/presets.ts` — アプリの状態、保存データの検証つき読み込み、トップの決定(`toTop`)、対局中のルール変更(`changeRules`)、デッキ名の変更(`renameDeck`)、対戦の種類(`mode`: 通常 / 大会 / オフィシャルトーナメント。`applyTournament` / `applyOpenRuleset` / `setMode` / `handPriorOf`)
- `core/prefs.ts` — 表示の設定(カードの絵を出すか)。対局の状態とは別のキーに保存する
- `core/collection.ts` — 手持ち(所持カードの ID の集合)の保存形式・テキスト入出力と、デッキの制限(★5 は 1 枚まで、★4 以上は合わせて 2 枚まで)
- `core/deckEval.ts` — デッキの評価。対戦開始時に起こりうる状況(シナリオ)の生成と、シナリオ 1 つを空の盤面から読み切るタスク `runDeckTask()`
- `core/deckPool.ts` — 探索の候補の絞り込み(相手のカードに対する採点)、出発点のデッキ、1 手先のデッキ
- `core/deckSearch.ts` — デッキ探索の進行(純関数の状態機械。`scheduler.ts` と同じ流儀)と、対戦条件から探索を組み立てる `prepareDeckSearch()`
- `core/deckRank.ts` — デッキの結果の日本語のラベルと注意書き
- `core/draftPick.ts` — オフィシャルトーナメントのドラフト(2 枚 + 2 枚 + 1 枚 のセットを 3 回選ぶ)。提示されたセットを、想定した相手に対する静的な点で並べる `rankDraftSets`
- `data/` — 同梱データ(`cards.json` / `npcs.json` / `sources.json` / `achievements.json` / `competitions.json`(大会 4 種) / `open-tournaments.json`(オフィシャルトーナメントのルール 10 件))と検索、対戦前に決まるルール(`preMatchRules`)、想定で埋める準備(`withPrior`)、数字 4 つからの逆引き、数字 → カード ID の対応づけ、カードの絵の ID と URL(`artIdOf` / `cardArtUrl`)、カードの入手方法(`cardSources`)、所有率とアチーブメントの判定(`ownershipPercent` / `achievementStatus`)。エンジンとソルバーは触らない(core は `data/` を import しない。ID とレアリティが要る所は `DeckCard` で受け取る)
- `worker/solver.worker.ts` — `runTask` を包むだけの殻。`hooks/useSolver.ts` がワーカープールを管理する
- `components/Tip.tsx` — 吹き出しの殻と位置決め(要素の下、入らなければ上)。`SourceTip` とルールの説明で共用
- `components/SourceTip.tsx` — 手持ちの画面で、マウスを乗せたカードの入手方法を出す吹き出し
- `components/RuleNames.tsx` — ルール名のボタンの並び(対局画面の上、NPC の注意書き)。乗せる・タップで説明が出る
- `components/TournamentPicker.tsx` — 大会(ランキング形式)の選択と相手(プレイヤー / カードバトルルームの NPC 5 人)
- `components/OpenRulesetPicker.tsx` — オフィシャルトーナメントのルールの組(ベーシック + アドバンス 5 種)
- `components/DraftPanel.tsx` — ドラフトの補助。ラウンドは自分の手札の埋まり具合から決め、提示された 3 セットを入れると `rankDraftSets` のおすすめが出る。最終ラウンドは 3 通りのデッキを `prepareDeckSearch`(evaluate、refine なし)で読み切って比べる(`DeckAdvisor` の `DeckResult` を共用)
- `hooks/useHoverTip.ts` — ルールの説明の吹き出しを出す決まり(手持ちの画面と同じ。手持ちの画面自体はタップのモードが絡むので使っていない)
- `components/AnalysisPanel.tsx` — おすすめの帯 `BestMove`(カード → マス、結論の一文、根拠、「打った」ボタン)と、他の手の一覧 `MoveTable`(`<details>`。スマホでは畳む)。文言は `core/rank.ts`(`outcomeText` / `KIND_GLOSS` / `secondaryText`)
- `components/Icon.tsx` — 対局画面のボタンに添える線画のアイコン(インライン SVG。色は文字色に従う)
- `components/Modal.tsx` — ネイティブ `<dialog>` の殻。`showModal()` でフォーカスの閉じ込め・Escape・背景の inert 化をブラウザに任せ、閉じる時に開く前の要素へフォーカスを戻す
- `components/OutcomeBar.tsx` — 勝ち・引き分け・負けの割合の帯(デッキの結果。数字は文で添える)
- `components/ConfirmAction.tsx` — 取り消せない操作(削除、置き換え、NPC を外す、対局中に設定へ戻る)の 2 段階ボタン。1 回目で「[本当に実行] [やめる]」に変わり、4 秒で戻る。`window.confirm` は自動操作を止めるので使わない
- `hooks/useCardArt.ts` — カードの絵を出すかのコンテキスト。`CardView` が直接読む(`memo` した `CollectionCell` の内側にも届かせるため、props では渡さない)
- `worker/deck.worker.ts` — `runDeckTask` を包むだけの殻。`hooks/useDeckSearch.ts` がワーカープールを管理する(`useSolver` の写し。`useSolver` は対局中の本線でテストが無いので、共通化していない)

### 設計上の重要な決定

- **エンジンは 2 本**: 読みやすい参照エンジンと探索用の高速エンジンを、ランダム生成の対局(3000 局)で毎手突き合わせる(`fastEngine.test.ts`)。ルールを変える時は**必ず両方**を直す。参照エンジンを UI の本番コードにしているのは、テスト専用コードとして腐らせないため。
- **設置処理の順序**: 設置 → セイム/プラス判定 + 通常支配 → (セイム/プラスで取れた場合のみ)コンボ連鎖 → タイプ枚数の更新。タイプアセンド/ディセンドは「置くカード自身を含めない枚数」で全ての比較を行い、解決が終わってから枚数を増やす。値は [1,10] にクランプ(「11」は A と同値で A に勝てない)。**表示はゲームに合わせて**、4 辺の数字は元のまま、ひし形の中央に `+1` / `−1` を出す(`CardView`。補正後の値やクランプは画面に出さない)。
- **勝敗は 10 枚で数える**: 盤面 9 枚 + 後攻の手元に残る 1 枚。
- **厳密探索を採る**: 既存の FF14 ツールはランダムプレイアウトで勝率を出すが、全深さのアルファベータがブラウザで十分速い(`npm run bench` の実測で、全窓探索の中央値は空の盤面 0.7 秒、1 枚後 0.12 秒、2 枚後 0.02 秒。セイム+プラス有効時)。遅いのは自分が先攻の初手だけなので、ワーカープールに初手ごとのタスクを配り、結果を逐次表示している。置換表と探索木内の手の並べ替えは、2 回の計測で効果が無いか逆効果だったので入れていない。盤面の対称性による削減は**無効**(カードは盤面と一緒に回転しない)。
- **同点の手の選び方**: 最善手は同点で並ぶことが非常に多いので、保証クラス → 「相手の応手のうち何通りで自分の勝ちが確定するか」(非公開手札では、具体的な手札の全列挙での勝ち数)→ 枚数差 → 静的評価、の順で選ぶ。期待値最大化(expectimax)は保証を守る前提と矛盾するので使わない。
- **非公開手札は上位集合で解く**: 相手は既知カードに加えて候補のどのカードでも出せるが、合計は不明スロットの数まで、という緩和。相手の選択肢を実際より増やすだけなので、「勝ち」と出ればどの具体的な手札に対しても勝ち(健全な最悪ケース保証)。候補が足りない時はこの探索をしない(相手の出すカードが尽きて値が壊れる)。足りない分は `HandPrior`(`core/handPrior.ts`)の想定から引いた手札で埋め、結果は「推定」。対局中(`worlds.ts` の `fill`)もデッキ評価(`deckEval.ts` の `ScenarioOptions.fill`、`Matchup.oppPrior`)も同じサンプラーで、`oppPool` は増やさない(候補 20 枚の上限と `MAX_HAND_CARDS` に触れない)。想定は 3 種: `level`(従来の 弱め/標準/強い。レアリティの重みでカードごとに引く)、`meta`(大会のプレイヤー。★5 1 枚 + ★4 1 枚 + ★3 以下 3 枚を、ルールを見た静的な強さの上位 10 / 10 / 20 枚から一様に。リバースでは小さい数字が上位に来る)、`draft`(ドラフトの相手。★1〜★5 を 1 枚ずつ、各段で 3 枚から最も強い 1 枚)。デッキの候補の採点(`deckPool`)は、想定から固定シードで引いた代表カード 60 枚(`Matchup.oppRef`)を相手にする。オールオープンでも想定で埋めた評価は「推定」(相手のデッキがその分布から来るという仮定の上)。
- **大会(ゲーム内)は 2 系統**(2026-09-22 にウェブで調査): (1) ランキング形式の大会は隔週で 4 種が順に開催され、ルールは固定(マンダヴィル・チャンピオンシップ = オールオープン + プラス、星神ニメーヤ賞典 = スリーオープン + スワップ、オーラン記念 = オーダー + セイム、ロウェナ商会杯 = ルーレット × 2)。名前はゲームデータ `TripleTriadCompetition`、ルールと入賞カードはシートに無いので `scripts/fetch-data.mjs` の `COMPETITION_FIXED` に手で持つ(出典は攻略 wiki とプレイヤーの対戦記録)。相手はオートマッチングのプレイヤー(デッキ不明 → `meta` の想定)か、カードバトルルームの NPC 5 人(`BATTLEHALL_NPCS`。`npcs.json` の場所で選ぶ)で、NPC 戦も大会のルールで行われるので `applyNpc` / `clearNpc` は大会モードではルールを変えない。勝ち点はプレイヤー戦 > NPC 戦、枚数差でも増えると報告されているが計算式は不明なので、勝ち点は出さない。(2) オフィシャルトーナメント(8 人・ドラフト)はゲームデータ `TripleTriadTournament` の 10 行(`Unknown0`〜`Unknown3` がルール ID。先頭は常にドラフト)で、ベーシック(ドラフトのみ)とアドバンス(+2 つ)が交互。設定画面の「対戦の種類」で切り替え、`SetupDraft.mode` / `tournamentId` / `openRulesetId` に持つ(保存形式は v1 のまま。古い保存は通常モードとして読む)。対局画面の見出し(`matchTitle`)に大会名を出す。ドラフト(`DraftPanel`)は「自分の手札」の代わりに出て、取ったセットのカードが `myCards` に入る(提示されたセットの入力は一時状態で、再読み込みで消えるのはそのラウンドの分だけ)。ドラフトのデッキはレアリティの制限を見ない(貸し出しのカード)。ドラフトの相手は `draft` の想定(★1〜★5 を 1 枚ずつ)。
- **相手の候補は対局画面に最初から並べる**(`PlayScreen` の `OppPool`): 不明スロットが残っている間、相手の手札の下に候補を 6 列で並べる(以前は「?」→ 一覧のダイアログ)。タップの意味は手番で変える。相手の番 = 出したカードとして**選ぶだけ**で、マスをタップした時に初めて `place`(`{from:'pool'}`)を 1 件記録する(押し間違えは押し直すだけ。以前は選んだ時点で `reveal` を記録していた)。自分の番 = オールオープン/スリーオープンで見えているカードを開く(`reveal`。開くと相手の実際の手札で読むので、「保証」が「確定」に変わり、推奨手も変わりうる。どれくらい差が出るかは測っていない)。オープンが「なし」なら自分の番は薄く出して押せない。スワップ中 = 来たカード。候補に無いカードは列の最後のマスのボタンから数字で入れる。手番で盤面が動かないよう、自分の番でも薄く出したままにし、案内は 1 行の見出しに収める(スマホ幅では候補の分だけ盤面が下がり、下の段が画面の外に出ることがある)。
- **開き間違いは「戻す」で直す**: 開いてまだ出していない相手のカードの下に「戻す」。`unrevealCard` が開いた記録を消し、後の記録の `{from:'revealed'}` の添字を 1 つ詰める(`reveal` は 1 件で 1 枚を `revealed` の末尾に足すので)。「戻した」記録は足さない(「1 手戻す」の対象も増えない)。出した・スワップで交換したカードは戻せない。開いたカードは `oppKnown` の末尾に付くだけなので、オーダーの「入力順 = 出す順」の前提は使えなくなる(`toPosition` は `view.revealed` が空の時だけ `oppOrderKnown` を渡す)。
- **スワップも対局画面で入れ替える**: 対戦が始まってから交換が分かるので、設定に戻らず `swap` イベント(自分の手札 1 枚 ↔ 相手の 1 枚)で入れ替える。相手の裏向きのカードが来た時は候補リストか手入力で決める(不明スロットが 1 つ減る)。来たカードは渡したカードと同じ位置に入れる(オーダーの並び。実機未確認)。交換した 2 枚は `view.revealed` に入れて、`CardRef` の 'revealed' でどちらの手札からも出せるようにする(設定の時点の持ち主と食い違うため)。盤面に 1 枚でも置いたら「スワップ」ボタンは出さない(交換は対戦開始時にしか起きない。気付くのが遅れたら「1 手戻す」で戻る)。
- **設定画面は作業の順に並べる**: 対戦相手 → 相手の手札(NPC から自動で入る) → この対戦のルール → 自分の手札(+保存デッキ) → デッキの評価と提案(`<details>` に畳む。探索中は開いたまま) → 対戦を始める。以前は提案機能が「相手の手札」より上にあり、下のセクションに依存していた。
- **NPC の「変更」は消さない**: 今の NPC を残したまま検索欄を出し、選んだ時点で `applyNpc` が丸ごと置き換える。外すのは別の「外す」ボタンで、ルールと相手の手札も消えるので `ConfirmAction` の 2 段階にする(以前は「変更」で全部が即座に消えた)。
- **札のタップは「変更」で統一**: 自分の札も相手の札もタップでエディタが開く。外すのは札の下の小さな「外す」ボタン(以前は相手の札だけタップ=外すで、添え字だけが違った)。相手の札を入れ替えた時、外れた札が NPC の可変カードなら候補へ戻す。
- **排他のルールはセグメントで見せる**: オープン(なし/スリー/オール)、タイプ(なし/アセンド/ディセンド)、出す順(自由/オーダー/カオス)。独立のチップに見えると両方選べると思われる。排他の処理は `toggleRule` のまま。`RuleChips` は設定画面と対局中の「ルールを変更」で共通。
- **説明文は短く、二次的な注意は `<details>` に**: ルーレット等の一般的な注意は NPC ごとの警告に一本化。デッキ結果の前提と注意は「前提と注意」に畳む。入力欄はプレースホルダーをラベル代わりにせず、`field-label` の可視ラベルを付ける(書式の例は入力すると消えるので欄の外に書く)。
- **先攻は対局画面で選ぶ**: FF14 では先攻が対戦開始後に決まるので、設定画面には置かず、対局画面の上で 1 枚目を置くまで切り替えられるようにしている(`setup.first` と下書きの `first` を両方書き換える)。置く前の `reveal` / `swap` は手番を見ないので、切り替えても記録は消えない。「はじめから」は `restartMatch()` で下書きから setup を作り直す(サドンデスの再戦中でも 1 局目に戻る)。
- **ルールは対局中にも変えられる**: 対局画面の上の「ルールを変更」から、設定画面と同じチップ(と、エースキラーの連鎖中の扱い)を切り替える。`changeRules()` が `setup.rules` / `setup.options` と下書きの `ruleIds` / `fallenAceInCombo` を両方書き換え、記録(events)は残す。置いたカードは `replay()` が新しいルールで計算し直す(`replay()` の検証はルールに依らないので、記録は途中で切れない)。下書きにも書くのは、「はじめから」が下書きから setup を作り直し、再読み込みが `options` を下書きから作り直すため。ゲームと食い違った時の直し方もこれ(「所有を修正」は 2026-09-22 に外した)。
- **ボタンは 4 段**: `btn-primary`(金のベタ塗り。画面に 1 つ)> `btn`(塗り+枠)> `btn-tertiary`(薄い枠だけ。閉じる/やめる/コピーなど)。取り消せない操作は `btn-danger`(赤の枠)+ `ConfirmAction`。文の中に置く時は `btn-sm`。文字だけの下線リンク風(旧 `btn-quiet`)は押せる所に見えないのでやめた。対局画面の「1 手戻す」「はじめから」「スワップ」はアイコン付きの `tool-btn`。無効状態は opacity で薄くしない(3.5:1 で読めない)。文字色を落として枠を消す。
- **金(`--gilt`)の役割は「おすすめ・主操作・選択中」だけ**: おすすめの輪、`btn-primary`、選択中のチップ/セグメント、盤の枠、コンボ等のバッジ、進捗バー。見出しは白(`--paper`)、フォーカスの輪は水色(`--focus`。金の輪と混ざらない)、注意の罫は琥珀(`--warn`)。押せる物の枠は `--line-strong`(mat-700 の上で 3.4:1)、区切り線だけ `--mat-line`。
- **文字サイズは 5 段**(`--fs-xs` 12px 〜 `--fs-xl` 28px)。本文は 16px。入力欄も 16px(iOS が拡大しないように)。指で押す端末(`pointer: coarse`)では押せる物を 44px 以上にする。
- **計算が終わるまでのおすすめは「暫定」と見せる**: 計算の途中で最善手が変わる(実測: 39% で「ヤ・シュトラ を 左下」、100% で「アレキサンダー・プライム を 右上」)。その間は「打った」ボタンを枠だけの金(`btn-provisional`、ラベル「暫定: この手を打った」)にし、「計算中 NN%」の札をおすすめの行に添え、帯の上端に進捗バーを敷く。終わると塗りの金(`btn-primary`)に変わる。Enter は暫定でも効く(ゲームで打った手を記録する操作なので)。
- **おすすめの帯は盤面のすぐ下**: 56rem 未満では自分の手札の直下(DOM の順)、以上では右列の上(`grid-template-areas`)。上から「カード を マス へ」→ 結論の一文(`outcomeText`。「この手なら引き分け以上です」など、保証できない種別では保証の言葉を使わない)→ 種別のバッジと説明(`KIND_GLOSS`)と数字 → ボタン。他の手の一覧は `<details>` で、スマホでは畳み、PC では開いて始める。表の数は「勝 / 分 / 負」に揃え、分母は上に 1 回だけ、分類のラベルはおすすめと違う行にだけ出す。`aria-live` は「カード を マス へ」の 1 行だけ(表まで包むと更新のたびに全部読まれる)。
- **手番の案内はタップする手札の隣**: 相手の番は相手の手札と盤面の間、自分の番は盤面と自分の手札の間に、持ち主の色の罫で出す(`turn-banner`)。枠は常に描いて、文が出たり消えたりしても盤面が動かないようにする。盤面のマスは `disabled` にしない(タブ順から消え、名前も読まれない)。置けない時は `aria-disabled` で、押しても何もしない。
- **盤面の札には持ち主の印**: 青と赤は輝度が同じで色相だけの区別になるので、盤面の札の左上に「自」「相」を出す(`CardView` の `ownerMark`。手札には出さない)。赤は青より暗くする。最後に置いた札は下辺の短い白いバー(選択中の白い outline と混ざらない)。フォーカスの輪は水色。
- **「設定を変える」は対局中なら 2 段階**: 記録を閉じる操作なので、置いたカードや開いたカードがあれば `ConfirmAction` で「対局を閉じて設定へ」を挟む。終局後と 1 枚も置いていない時はそのまま移る。記録を残したまま設定画面へ行けるようにしないのは、下書きと対局中の設定を食い違わせないため。終局後の「同じ相手ともう一戦」は設定画面を経由せず「はじめから」と同じく新しい対局を始める。「対戦を始める」と再戦の開始では先頭にスクロールする(設定画面の途中の位置のまま対局画面が出ていた)。
- **Enter は「おすすめ通りに打った」、R は「はじめから」**: Enter はボタンが出ている時だけ、R は「はじめから」が押せる時だけ効く。どちらもダイアログが開いている時(`dialog[open]`)と文字の入力中は効かない(keydown の処理は 1 つにまとめてある)。マウスで押したボタンにはフォーカスが残るので、`preventDefault()` でそのボタンが Enter でもう一度押されるのを止めている。「はじめから」はボタンと同じく確認を挟まないので、対局中に R を誤って押すと記録は戻らない。
- **「確定」「保証」「推定」のラベルを混ぜない**: 保証できないもの(候補不明の手札、カオス)に保証の言葉を使わない。具体的な手札の列挙は、各世界で相手の手札を知っている前提になるので楽観側の推定。
- **ワーカーの中断は terminate**: 同期処理の探索はメッセージでは止められず、`SharedArrayBuffer` は GitHub Pages では必要なヘッダーを設定できない。局面が変わったら計算中のワーカーを破棄して作り直す。
- **画面反映の間引きはタイマー**: `requestAnimationFrame` は非表示のタブで発火しないため、ゲームの横で裏に回っている間に更新が止まる。
- **計算の進み具合(%)は後戻りさせない**: どちらも「完了した時だけ 100%」で、表示は切り捨ての整数(`progressPercent`)。
  - 対局中の手(`scheduler.ts` の `progressRatio`): 済んだ計算量 ÷ (済んだ計算量 + 残りの上限)。タスク数の done / total は、分類が終わった所で同点の手の順位付けが加わって分母が増え、後戻りする(95% → 51% を実際に踏む)。分類が済んでいない手は「この後に来うる最も重いタスク」まで残りに数え、分かった分だけ減らす。重みは分類 1 件を 1 として、応手の数え上げ(`mistakes`)を 4(空の盤面からの実測で分類の 2〜39 倍、中央値 3.5 倍)、それ以外を 1。
  - デッキの探索(`deckSearch.ts` の `deckSearchRatio`): 段階(seeds / climb / refine / certify)ごとに見込みの計算量で持ち分を割り振り、段階の中は片付いた割合で進める(climb は片付いたデッキ数 ÷ 予算)。上限の方式(全デッキを打ち切らずに調べる前提)は、打ち切りが効くと序盤が遅すぎた(強い NPC で 51 秒で 2%)。climb のデッキ 1 つの重さは、出発点の 0.3 倍と見込む(`CLIMB_COST`。イソベでの実測は 0.2〜0.35 倍で、後半ほど軽い。この 1 例しか測っていない)。全勝のデッキが見つかった時や行き止まりでは、残りの持ち分を飛ばして先へ進む。
- **トップは対局画面**(`toTop`): 開いた時とロゴは対局画面へ。対局中なら記録はそのまま、そうでなければ下書きから新しい対局を始める(開くたびに「対戦を始める」を押さずに済むように)。自分の手札が 5 枚揃っていない(初めて開いた時など)なら設定画面。保存形式は変えていない(起動時の `usePersisted` の読み込みに `toTop` を挟むだけ)。ロゴは手持ちの画面も閉じる。`<a href={BASE_URL}>` なので、Ctrl/⌘ + クリックは新しいタブで開く。「手持ち」のボタンは対局中も出す(手持ちの画面を開いている間は `PlayScreen` が外れ、戻ると計算をやり直す。記録は消えない)。
- **ルールの説明はホバーで出す**: 設定画面と「ルールを変更」のチップ・セグメント(「なし」「自由」にも)、対局画面の上のルール名、NPC の注意書きのルール名。文は `RULE_HELP`(ゲーム内の説明文 = XIVAPI の `TripleTriadRule.Description` とエンジンの実装に合わせ、初めての人に分かる言葉で書いた)。チップのタップはルールの切り替えなので、タッチの端末向けに `RuleChips` の下へ `<details>`「ルールの説明」を添える。ルール名のボタンはタップでも出る。吹き出しは `RuleChips` の中に描く(`<dialog>` の最上位レイヤーの外に描くとダイアログの下に隠れる)。Escape は吹き出しだけを消す(`preventDefault` でダイアログまで閉じないようにしている。ヘッドレス Chrome に CDP で実キーを送って、1 回目で吹き出しだけ、2 回目でダイアログが閉じるのを確認した)。NPC の検索結果の行(行全体が選ぶボタン)には付けていない。
- **状態の更新関数に副作用を入れない**: StrictMode では更新関数が 2 回走り、カードが二重に確定する(`CardEditor` で実際に踏んだ)。
- **手持ちはカード ID で保存する**: 保存済みデッキは `CardDef`(数字とタイプ)だが、手持ちは数字もタイプも同じ別カードが 6 組あるので ID で持つ(`triadnavi:collection:v1`)。同梱データに無い ID も捨てない(データを古い版に戻しても所持が消えないように)。手持ちの画面は `AppState.phase` に入れず `App.tsx` の一時的な状態にしている(`parseAppState` は未知の phase を対戦前に落とすので、保存形式を変えずに済む)。
- **カードリストの並びは ID 順ではない**: ゲーム内の番号は `order`(`ex` なら Ex. 番号)。475 枚中 408 枚で ID と食い違うので、手持ちの画面は `CARDS_IN_LIST_ORDER` で並べる。
- **デッキは「対戦開始時のシナリオ」で評価する**: シナリオ = ルーレットの結果 × 相手の具体的な手札 × 先攻/後攻 × スワップの交換 ×(オーダーなら)相手の並び順 ×(カオスなら)双方の出る順。デッキに依らないリストを対戦条件のキーをシードに 1 回だけ作り、全デッキを同じリストで採点する(共通乱数。サンプリングが入ってもデッキ同士の比較に乱数の差が乗らず、同じ条件なら何度やっても同じ結果)。全ての組み合わせが上限以下なら全列挙、超えたら層別サンプリングし、上位 3 デッキだけを大きいリストで測り直す(`scenarioBudget`)。
- **デッキの比べ方**: 勝ちが確定する確率 → 引き分け以上が確定する確率 → 負ける時の枚数差の小ささ → 静的な点。強い NPC(デッキ制限に縛られず ★5 を何枚も使う)には、相手が最善を尽くすとどのデッキも勝てないので、負けの浅さまで見ないと差がつかない。シナリオ 1 つは安い順に「勝ちの探り → 引き分けの探り → 負けの深さ」の 3 段階で調べ、前の段階で決まれば後は省く(勝ちが外れて引き分けが当たれば、保証値はちょうど 0)。比べる順と調べる順が同じなので、勝ちの数で上回れない候補は重い探索を 1 回もせずに落とせる。
- **探索は「見つかった中で最良」**: 合法なデッキは手持ち 200〜475 枚で 10^9〜10^11 通り。相手のカードに対する採点でレアリティの枠ごとに候補を絞り、出発点から 1 枚の入れ替え(オーダーなら位置の入れ替えも)で登る。「最善のデッキ」とは言わない。
- **支配による絞り込みは使わない**: 「全ての辺が同じか上回るカードに替えれば悪くならない」は、基本ルールだけでも成り立たない(取られたカードは相手のためにその高い数字で守る。反例は `deckPool.test.ts`)。ランダムな局面での実測では、強いカードに替えて保証値が下がる割合は基本ルールで約 0.1%、セイム+プラスで約 5%。
- **オーダーの相手の並びはサンプリングする**: 「相手は好きな順に出せる」という緩和は健全だが、実在の NPC で測るとどのデッキも勝ち 0・引き分け 0 になり比較にならなかった(NPC も並び順に縛られる)。並び順はランダムと見なしてシナリオに展開し、結果は「推定」と表示する。上位集合の保証(`certify`)では相手の順は自由のまま。
- **探索の決定性は世代のバリアで保つ**: 世代ごとに、比べる相手とシナリオを調べる順を固定し、世代の全デッキが片付いてから次へ進む。デッキが片付く条件(最後まで評価、または上回れないと確定)は真の値だけで決まるので、ワーカーの数や結果の届く順が違っても同じデッキに行き着く。予算は時間ではなくデッキ数で数える。タスクの記録は世代が替わるたびに消す(溜め続けると結果 1 件ごとのコピーが重くなり、実測で 15 倍遅くなった)。
- **デッキの評価の速さ(`npm run bench:deck` の実測)**: 勝ちの探りは中央値 50〜160ms、負けの深さまでの探索は 0.3〜0.6 秒(セイム/プラスや強い NPC で重い)。オーダーとカオスは出すカードが決まっているので 1 シナリオ 1〜5ms。上位集合の探りは 30〜520ms。ブラウザの実測では、セイム+プラスの NPC で 28 秒に 79 デッキ、オーダーで 20 秒に 257 デッキ。最悪は強い NPC のスワップ(24 シナリオ)で 1 デッキ 2〜3 秒。
- **カードの絵は XIVAPI への直リンクで、同梱しない**: カード 1 枚ごとの絵の公式な配布元は無い(調査済み: スクウェア・エニックスに公開 API は無く、Lodestone のエオルゼアデータベースは 475 枚すべてが共通のアイテムアイコン 3 種、公式プレイガイドにも公式ファンキットにも無い)。絵は XIVAPI v2 のアセット変換(`/api/asset?path=ui/icon/087000/0870NN.tex&format=webp`。JPEG は透過が黒く潰れる)を `<img>` で実行時に読む。XIVAPI はゲームのパッチから取り出したファイルを配っている有志のサービスで、スクウェア・エニックスの著作物利用条件が挙げる素材(スクリーンショット、公式サイトの画像、ファンキットなど)の範囲外だと分かった上での判断。リポジトリにも `dist` にも画像を入れないので、やめる時は `CardView` の `<img>` を消すだけでよい。アイコン番号は 87000 + カード ID(XIVAPI のシートに絵の項目は無いので `cardArtUrl` で組み立てる。`fetch-data.mjs` と `cards.json` は変えていない)。`CardDef` は ID を持たないので `artIdOf` で引くが、数字が同じカードが複数ある 9 組(タイプも同じ 6 組 + タイプだけが違う 3 組)は名前が一致した時だけ絵を出す(数字を打って確定したカードは名前が「A / B」になり絵なし。違う絵を出すより出さない方を採る)。タイプで絞って 1 枚に決めてはいけない(タイプが効かないルールでは `CardEditor` が先頭の候補のタイプで確定する)。`DeckAdvisor` は手札を ID に対応づける時に名前を推測で書き換えるので、この 9 組では絵も表示中の名前に合わせた推測になる。読み込みに失敗したら数字だけの表示に戻る(失敗は真偽値ではなく ID で覚える。同じ `CardView` が後で別のカードを映すため)。数字の下の暗い円などの強調は `.has-art` の時だけ掛かり、絵が無い時の見た目は変えない。「カードの絵を表示」(`triadnavi:prefs:v1`、既定はオン)を切ると `<img>` を描かないので、XIVAPI への通信は 0 件になる。同じ理由で `index.html` に preconnect を置かない。
- **データの更新は手動**: CI では第三者 API を叩かない(API の停止でデプロイが壊れないように)。NPC のデッキとルールはゲームデータ由来の XIVAPI を正とする。ルーレットが 2 枠ある NPC は `rules: [1, 1]` になる。大会の名前(`TripleTriadCompetition`)とオフィシャルトーナメントのルール(`TripleTriadTournament`)も XIVAPI から取る。後者のフィールド名 `Unknown0`〜`Unknown3` は EXD のスキーマ更新で変わりうる(変わるとスクリプトが止まるので、`OPEN_URL` と `OPEN_FIELDS` を直す)。XIVAPI は 522 を返すことがある(再実行でよい)。
- **カードの入手方法は FFXIV Collect の記載を日本語にそろえて同梱する**(`sources.json`、1 行 1 枚。`cards.json` の形は変えていない)。日本語を指定しても分類名と一部の文章(FATE のジェム交換、エウレカ、ディープダンジョン、友好部族など 43 種類)が英語で返るので、分類名は `fetch-data.mjs` の `KIND_JA`、文章は `scripts/source-ja.json`(英語の原文 → `{text, where}`)で置き換える。訳の名前は XIVAPI のゲームデータを英語名で引いた日本語名で、判断が要るもの(ディープダンジョンの「Silver Sack」が埋もれた財宝の何番か、など)は日本の攻略サイトでも確かめた。表に無い英語は英語のまま残して警告を出す(更新は止めない)。新しいパッチで警告が出たら表に足すこと。パックはカードの記載が「Dream Triad Card」だけなので、パックの API の名前と値段に置き換える(値段 0 のプラチナは大会の賞品)。NPC は `related_id` で NPC の API の場所と座標を付ける。
- **入手方法はマウスとキーボードでは hover/フォーカスで、タッチでは「タップで: 入手方法を見る」に切り替えて出す**: スマホのタップでも `pointerenter` は来るが、出すと次に触るまで残るので `pointerType === 'mouse'` で絞っている。タッチ向けの切り替え(`coll-tapmode`)はモードを `useRef` に持ち(`memo` した `CollectionCell` の `onTap` を作り直さないため)、表示用の state を鏡にする。タップで出した吹き出しは、同じカードをもう一度タップするか、カード以外に触るか、スクロールで消える。マウスがある端末では切り替えを隠す。アチーブメントは読むだけなので `<details>` に畳み、絞り込みとカード一覧を先頭に近づける。フォーカスでは `:focus-visible` の時だけ出す(マウスで押した後に残るフォーカスで出すと、離れても消えなくなる)。吹き出しは画面に 1 つだけ置き、描いた直後に大きさを測って位置を要素に直接書く(カードの下、入らなければ上。左右は画面の中)。乗せてから 250ms で出し、出ている間に隣へ移れば待たずに切り替える。スクロールすると位置がずれるので消す。非表示のタブではタイマーが 1 秒ほどに間引かれるので、自動操作で確かめる時は待ち時間を長めに取る。
- **所有率とアチーブメントは手持ちから判定する**: 手持ちの画面に「所有率」(同梱データのカード全体に対する所持枚数の割合。小数 1 桁で切り捨て。四捨五入だと 474 / 475 枚で 100.0% になる)と、アチーブメントを未達成も含めて全て出す(achievements.json の並び。未達成は「所持 / 必要」と残りの枚数、細いバー)。アプリが知っているのは所持カードだけなので、判定できる 2 種類だけを扱う: 「カードコレクター：ランク1〜14」(カードを N 種類入手する。Ex. も数える)と「生粋のカードマニア」(No.1〜No.312 をすべて入手する)。NPC への勝利・大会・ルーレットの系統は記録が無いので出さない。データは `fetch-data.mjs` が FFXIV Collect のアチーブメントから説明文の形で選んで `achievements.json` に書く。文言が変わって件数が減ったら例外で止まる(黙って空にならないように)。
- **対戦相手(NPC)の顔は出さない**(2026-09-22 に調査済み): ゲームデータ(XIVAPI の `TripleTriad` / `TripleTriadResident`)にも FFXIV Collect にも NPC の画像は無い。Console Games Wiki には約 85% の NPC のスクリーンショットがあるが、Cloudflare の直リンク防止で、外部サイトをリファラーにすると 403 になる(リファラーを消せば通るが、運営者の拒否を回避することになるので採らない)。ARR:TT(arrtripletriad.com)は防止していないが、個人運営で許可は無く、7.0 前後で更新が止まっていて試した 16 人中 7 人しか無かった。

### 実機で未確認の挙動(どの情報源にも検証例が無い)

食い違った時は、対局画面の「ルールを変更」でルール(エースキラーの連鎖中の扱いを含む)を切り替えると、置いたカードを計算し直す。盤面の持ち主を手で直す「所有を修正」は外した(core の `setOwner` イベントと「食い違いをコピー」は、保存済みの対局を再生するために残している。新しい対局では出ない)。確定したら該当箇所とテストを直すこと。

- コンボ連鎖中のエースキラー(`EngineOptions.fallenAceInCombo`、既定は有効)
- サドンデス再戦時の先攻(交互と仮定。再戦開始時に選べる)
- 置いたカードが通常支配したカードは、コンボの連鎖の起点にならない(FFTriadBuddy と同じ順序)
- エースキラーはタイプ補正後の値で判定する
- NPC の可変カードの選ばれ方(どの組み合わせも同じ確率と仮定)
- ルーレットで加わりうるルールとその確率(`ROULETTE_RULE_IDS` のどれもが同じ確率、既に有効なルールとそれと排他のルールは出ない、と仮定)
- スワップで交換されるカードの選ばれ方(双方の 5 枚から同じ確率)と、交換で来たカードがオーダーで元のカードと同じ位置に入るか
- オーダーでの NPC の並び順(ランダムと仮定)
- 大会の NPC 戦(カードバトルルームの 5 人)に大会の固定ルールが適用される(プレイヤーの対戦記録 1 件のみ)。4 大会のルールが固定(攻略 wiki とプレイヤーの記録で一致。ゲームデータには無い)
- 大会の勝ち点の計算式(プレイヤー戦 > NPC 戦、枚数差で増える、としか分かっていない)
- ドラフトの手札の構成(★1〜★5 が 1 枚ずつ、はコミュニティ情報)と提示のされ方(3 セット × 3 回で 2 枚 + 2 枚 + 1 枚、は 5.4 パッチノート)

「リバース時はエースキラーが無効」と書く攻略サイトがあるが、ゲーム内テキスト(「リバース適用時は1をAで支配できる」)と矛盾するので採用していない。

## Deployment

GitHub Pages(GitHub Actions、`deploy-pages@v4`)。push to main で自動デプロイ(test → lint → build)。

- `vite.config.ts` の `base: '/triadnavi/'` は**リポジトリ名と一致必須**
- `index.html` の `darkreader-lock` メタは Dark Reader 拡張の再配色防止。削除しないこと
